import React from "react";
import ReactDOM from "react-dom/client";
import App from "../virtual_meeting_ui.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
